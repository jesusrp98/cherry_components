name: cherry_components
description: A new Flutter package project.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  google_fonts: ^0.4.0
  row_collection:
  expand_widget:
  cached_network_image:
  flutter_svg:

dev_dependencies:
  flutter_test:
    sdk: flutter
